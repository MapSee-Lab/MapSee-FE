# Mapsy 통합 PRD v1.0

**프로젝트명**: Mapsy (맵시)
**문서 유형**: 통합 Product Requirements Document
**작성일**: 2026-01-20
**버전**: 1.0
**대상 독자**: Flutter 개발자, UI/UX 디자이너, PM, Backend 개발자

---

## 📑 목차

### Part 1: Executive Summary (프로젝트 개요)
1. [프로젝트 비전](#1-프로젝트-비전)
2. [핵심 기능](#2-핵심-기능-6개)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [기술 스택](#4-기술-스택)
5. [마일스톤](#5-마일스톤-로드맵)

### Part 2: MVP 프론트엔드 상세 명세 (Riverpod 중심)
1. [인증 플로우](#part-2-mvp-프론트엔드-상세-명세)
2. [홈 피드 화면](#22-홈-피드-화면)
3. [AI 추출 플로우](#23-ai-추출-플로우)
4. [장소 상세 화면](#24-장소-상세-화면)
5. [북마크 관리 화면](#25-북마크-관리-화면)
6. [디자인 시스템](#26-디자인-시스템)

### Part 3: 백엔드 API 간결 정리
1. [API 엔드포인트 목록](#part-3-백엔드-api-간결-정리)
2. [페이지네이션 전략](#32-페이지네이션-전략)
3. [인증 플로우](#33-인증-플로우)

### Part 4: 확장 계획 및 부록
1. [Phase 3-5 개요](#part-4-확장-계획)
2. [향후 로드맵](#42-향후-로드맵)
3. [용어 정리](#부록-a-용어-정리)

---

# Part 1: Executive Summary

## 1. 프로젝트 비전

### 1.1 문제 정의
**SNS 속 맛집/핫플레이스 정보 파편화**
- Instagram 릴스, YouTube 쇼츠에 장소 정보가 흩어져 있음
- 영상을 보고 장소명을 수동으로 검색/저장하는 번거로움
- 저장한 장소를 체계적으로 관리하기 어려움
- 트렌드 장소 발견이 어려움

### 1.2 해결책
**AI 기반 SNS 장소 자동 추출 + 스마트 북마크 플랫폼**
- Instagram/YouTube URL 입력 → AI가 장소명/주소 자동 추출
- 추출된 장소를 폴더별로 정리 (가고 싶은 곳/가본 곳)
- 실시간 트렌드 키워드 및 인기 장소 피드 제공
- 메모/별점/방문 기록 기능으로 개인화된 장소 관리

### 1.3 타겟 사용자
**Primary (1차)**: 20-30대 여성 (맛집/카페 탐방 관심층)
**Secondary (2차)**: 여행 계획자, 커플 (데이트 코스 검색)
**Tertiary (3차)**: 인플루언서 (컨텐츠 제작용 장소 수집)

---

## 2. 핵심 기능 (6개)

### 2.1 AI 장소 추출
**기능**: SNS URL → AI 분석 → 장소 정보 자동 추출
**지원 플랫폼**: Instagram (릴스/게시물), YouTube (쇼츠/영상)
**추출 정보**: 장소명, 주소, 좌표, 카테고리, 설명
**처리 방식**: 비동기 (즉시 응답 + 백그라운드 처리 + 폴링)
**정확도 목표**: Instagram 85%+, YouTube 80%+

### 2.2 홈 피드
**섹션 1**: 최신 추가 장소 (무한 스크롤, Cursor 페이지네이션)
**섹션 2**: 인기 장소 (가중치 알고리즘: 최근 7일 = 2배)
**섹션 3**: 내 저장 장소 TOP (최대 10개)
**섹션 4**: 트렌드 키워드 (7일 기준 상승률)

### 2.3 키워드/태그 피드
**기능**: 특정 키워드 관련 장소 탐색
**필터**: 최신순/인기순/거리순
**뷰 모드**: 그리드뷰(2열) / 리스트뷰 전환
**정보 표시**: 키워드 통계 (장소 개수, 트렌드 점수, 상승률)

### 2.4 장소 상세 정보
**기본 정보**: 이름, 카테고리, 평점, 주소, 전화번호
**영업 정보**: 현재 상태 (영업 중/종료), 영업시간 (요일별)
**위치 정보**: 지도 미리보기, 길찾기 (카카오맵/네이버맵/구글맵)
**추가 정보**: 관련 키워드, 이미지 갤러리, 출처 콘텐츠 링크

### 2.5 개인 북마크 관리
**폴더 구조**: 전체/가고 싶은 곳/가본 곳/커스텀 폴더
**메타 정보**: 메모 (500자), 별점 (1-5), 방문 날짜
**정렬/필터**: 폴더별, 방문 여부, 별점, 최근순
**액션**: 스와이프 삭제/편집, 바텀 시트 메뉴
**Soft Delete**: 30일 복구 가능

### 2.6 소셜 기능 (Phase 2)
**컬렉션 공유**: 폴더별 장소 목록 공유
**팔로우**: 다른 사용자의 장소 피드 구독
**좋아요/댓글**: 장소 추천 상호작용

---

## 3. 시스템 아키텍처

### 3.1 전체 구조
```
┌─────────────────────────────────────────────────────────┐
│                  Flutter Mobile App                      │
│              (iOS/Android - Riverpod)                    │
└─────────────────┬───────────────────────────────────────┘
                  │ REST API (HTTPS)
                  ↓
┌─────────────────────────────────────────────────────────┐
│           Spring Boot Backend (4.0.1)                    │
│              Multi-Module (7 modules)                    │
├─────────────────┬───────────────┬───────────────────────┤
│   PostgreSQL    │     Redis     │   Firebase           │
│   (Main DB)     │  (Cache/Session) │ (Auth/FCM)        │
└─────────────────┴───────────────┴───────────────────────┘
                  │ Webhook Callback
                  ↓
┌─────────────────────────────────────────────────────────┐
│            FastAPI AI Server (Python 3.13)               │
├─────────────────────────────────────────────────────────┤
│ yt-dlp (SNS 다운로드) | Whisper (STT)                    │
│ Gemini 2.5 Flash (LLM) | Kakao Maps (Geocoding)         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 데이터 플로우 (AI 추출 예시)
```
1. 사용자 URL 입력
   ↓
2. Flutter → Backend POST /api/content/analyze
   ↓
3. Backend → AI Server (비동기 요청)
   ↓
4. 즉시 응답 (contentId 반환)
   ↓
5. AI Server 백그라운드 처리
   - SNS 다운로드 (yt-dlp)
   - 음성 추출 (STT: Whisper)
   - 영상 OCR (Tesseract)
   - LLM 분석 (Gemini 2.5 Flash)
   - 좌표 변환 (Kakao Geocoding)
   ↓
6. AI Server → Backend Webhook /api/ai/callback
   ↓
7. Flutter 폴링 (2초 간격)
   - GET /api/content/{contentId}
   - status: COMPLETED → 결과 표시
```

---

## 4. 기술 스택

### 4.1 Frontend (Flutter)
```yaml
Flutter SDK: ^3.9.2
언어: Dart

# 상태 관리 (Riverpod 코드 생성 패턴)
flutter_riverpod: ^2.6.1
riverpod_annotation: ^2.6.1
riverpod_generator: ^2.6.2

# 라우팅
go_router: ^17.0.1

# 네트워크
dio: ^5.9.0
retrofit: ^4.7.2
retrofit_generator: ^9.1.8

# 코드 생성
build_runner: ^2.4.14
freezed: ^2.5.7
json_serializable: ^6.9.2

# 인증
firebase_auth: ^6.1.3
google_sign_in: ^6.2.3
sign_in_with_apple: ^6.1.3

# UI
flutter_screenutil: ^5.9.3 (반응형)
smooth_page_indicator: ^1.2.1
flutter_svg: ^2.2.1

# 알림
firebase_messaging: ^16.0.1
flutter_local_notifications: ^19.4.2
```

### 4.2 Backend (Spring Boot)
```
Spring Boot: 4.0.1
Java: 21
빌드 도구: Gradle

주요 모듈:
- MS-Common (공통 유틸리티)
- MS-Member (회원 관리)
- MS-Place (장소 관리)
- MS-Content (콘텐츠/AI 추출)
- MS-Web (API 엔드포인트)

데이터베이스:
- PostgreSQL (메인 DB)
- Redis (캐시 + 세션)

인증:
- Firebase Admin SDK
- JWT (Access Token 1시간, Refresh Token 7일)
```

### 4.3 AI Server (FastAPI)
```
Python: 3.13+
FastAPI: 최신

AI/ML:
- Google Gemini 2.5 Flash (LLM)
- Faster-Whisper (STT)
- Tesseract (OCR, 한글 언어팩)

외부 API:
- YouTube Data API v3
- Kakao Maps API (Geocoding)
- Nominatim (Fallback Geocoding)

기타:
- yt-dlp (SNS 콘텐츠 다운로드)
- ffmpeg/ffprobe (영상 처리)
```

---

## 5. 마일스톤 로드맵

### Phase 1: MVP 개발 (Week 1-12) 🟡 진행 중
**목표**: 핵심 기능 구현 및 베타 테스트

| 마일스톤 | 담당 | 기간 | 상태 |
|---------|------|------|------|
| M1: Backend API 완성 | Backend Team | Week 1-4 | 🟡 70% |
| M2: AI 서버 안정화 | AI Team | Week 2-5 | 🟡 60% |
| M3: Flutter 앱 개발 | Frontend Team | Week 3-8 | 🔴 0% |
| M4: 베타 테스트 | QA Team | Week 9-10 | 🔴 0% |
| M5: 1차 MVP 출시 | DevOps Team | Week 12 | 🔴 0% |

**주요 기능**:
- ✅ Firebase OAuth (Google/Apple)
- ✅ Instagram/YouTube 장소 추출 (AI)
- ✅ 홈 피드 (최신/인기/트렌드)
- ✅ 장소 상세 정보
- ✅ 북마크 관리 (폴더/메모/별점)

### Phase 2: 기능 확장 (출시 후 3개월)
**목표**: 소셜 기능 + 플랫폼 확장

- 컬렉션 공유
- 팔로우/좋아요/댓글
- TikTok/Facebook/Twitter 지원
- 푸시 알림 (장소 추천, 트렌드)

### Phase 3: 수익화 (출시 후 6개월)
**목표**: 프리미엄 구독 + B2B API

- 프리미엄 구독 (월 9,900원)
- 무제한 AI 추출
- B2B API 제공 (마케팅 업체)

---

# Part 2: MVP 프론트엔드 상세 명세

## 2.1 인증 플로우 (4개 화면)

### 2.1.1 스플래시 화면 (Splash Screen)

#### 화면 구성
```
┌─────────────────────────┐
│                         │
│                         │
│      [Mapsy Logo]       │
│      맵시 (Gradient)    │
│                         │
│   "SNS 속 맛집을 찾다"   │
│                         │
│                         │
└─────────────────────────┘
```

#### 로직 흐름
```
앱 시작
  ↓
main.dart 초기화
  - Firebase 초기화
  - Crashlytics 설정
  - FCM 초기화
  ↓
SplashPage 렌더링
  - 로고 애니메이션 (0.8초 페이드인)
  - 서브 타이틀 애니메이션 (1초 지연)
  ↓
authStateProvider 확인
  - StreamProvider로 Firebase Auth State 감지
  - User? currentUser
  ↓
분기 처리
  - User == null → LoginPage로 이동
  - User != null → HomePage로 이동
  ↓
최소 표시 시간 (2초) 보장
```

#### Provider 구조
```
authStateProvider (StreamProvider)
  → Firebase.authStateChanges() 스트림 구독
  → User? 반환
  → GoRouter refreshListenable로 연결
  → 상태 변경 시 자동 리다이렉트
```

---

### 2.1.2 온보딩 슬라이드 (Onboarding Slides)

#### 화면 구성 (3페이지)
```
[페이지 1: AI 추출 기능]
┌─────────────────────────┐
│    [일러스트 이미지]      │
│   SNS URL → AI 분석     │
├─────────────────────────┤
│  SNS 속 맛집,           │
│  AI가 찾아드려요         │
│  인스타그램 릴스나       │
│  유튜브 영상 URL만       │
│  붙여넣으면 끝!          │
│  ● ○ ○                 │
│  [건너뛰기]    [다음 →] │
└─────────────────────────┘

[페이지 2: 북마크 기능]
┌─────────────────────────┐
│    [일러스트 이미지]      │
├─────────────────────────┤
│  저장하고, 정리하고,     │
│  공유하세요              │
│  폴더별로 장소를 정리하고 │
│  메모와 별점을 남겨요     │
│  ○ ● ○                 │
│  [건너뛰기]    [다음 →] │
└─────────────────────────┘

[페이지 3: 피드 기능]
┌─────────────────────────┐
│    [일러스트 이미지]      │
├─────────────────────────┤
│  트렌드 장소를           │
│  먼저 만나세요            │
│  지금 뜨는 장소와         │
│  키워드를 한눈에          │
│  ○ ○ ●                 │
│  [건너뛰기]  [시작하기]  │
└─────────────────────────┘
```

#### 로직 흐름
```
OnboardingPage 진입
  ↓
PageView 초기화
  - initialPage: 0
  - PageController 생성
  ↓
페이지 전환
  - 스와이프 → PageController.nextPage()
  - "다음" 버튼 → PageController.animateToPage(currentPage + 1)
  ↓
페이지 인디케이터 업데이트
  - SmoothPageIndicator 자동 동기화
  ↓
"시작하기" 버튼 (3페이지)
  ↓
SharedPreferences 저장
  - onboardingCompleted = true
  ↓
LoginPage로 이동
```

#### Provider 구조
```
onboardingStatusProvider (FutureProvider)
  → SharedPreferences.getBool('onboardingCompleted')
  → bool 반환
  → GoRouter redirect에서 확인
    - false → OnboardingPage 강제 이동
    - true → 정상 플로우
```

---

### 2.1.3 로그인/회원가입 화면 (Login Page)

#### 화면 구성
```
┌─────────────────────────┐
│                         │
│      [Mapsy Logo]       │
│                         │
├─────────────────────────┤
│  [G] Google로 시작하기   │
│  [] Apple로 시작하기     │
├─────────────────────────┤
│  ☑ 서비스 이용약관 및    │
│     개인정보 처리방침     │
│     동의 (필수)          │
│  ☐ 마케팅 정보 수신      │
│     동의 (선택)          │
│  [약관 보기]  [시작하기] │
└─────────────────────────┘
```

#### 로직 흐름 (Google 로그인 예시)
```
"Google로 시작하기" 버튼 클릭
  ↓
authNotifierProvider.notifier.signInWithGoogle() 호출
  ↓
AuthNotifier 상태 변경
  - state = AsyncValue.loading()
  ↓
FirebaseAuthDataSource.signInWithGoogle()
  - GoogleSignIn().signIn()
  - GoogleAuthCredential 생성
  - FirebaseAuth.signInWithCredential()
  ↓
Firebase ID Token 검증
  - currentUser.getIdToken()
  - 실패 시 → 세션 정리 (_cleanupSessionOnFailure)
  - FirebaseAuthException 발생 (code: 'token-validation-failed')
  ↓
성공 시
  - state = AsyncValue.data(userCredential.user)
  ↓
authStateProvider 자동 업데이트
  - GoRouter refreshListenable 트리거
  ↓
redirect 함수 실행
  - isAuthenticated = true
  - HomePage로 자동 이동
  ↓
실패 시
  - FirebaseAuthErrorHandler.createAuthException()
  - state = AsyncValue.error(AuthException)
  - UI에서 에러 메시지 표시
```

#### Provider 구조
```
authNotifierProvider (NotifierProvider<AuthNotifier, AsyncValue<User?>>)
  ↓
AuthNotifier
  - state: AsyncValue<User?>
  - signInWithGoogle()
  - signInWithApple()
  - signOut()
  - _validateIdToken() (내부)
  - _cleanupSessionOnFailure() (내부)
  ↓
firebaseAuthDataSourceProvider (Provider<FirebaseAuthDataSource>)
  - FirebaseAuthDataSource 싱글톤
  - signInWithGoogle()
  - signInWithApple()
  - signOut()
  - getIdToken()
  - currentUser
  - authStateChanges()
```

#### 에러 처리 흐름
```
[FirebaseAuthException 발생]
  ↓
FirebaseAuthErrorHandler.createAuthException()
  - code별 매핑
    - 'user-cancelled' → "로그인을 취소했습니다"
    - 'network-request-failed' → "네트워크 오류가 발생했습니다"
    - 'invalid-credential' → "인증 정보가 유효하지 않습니다"
    - 기타 → "알 수 없는 오류가 발생했습니다"
  ↓
AuthException 생성
  - message: 사용자 친화적 메시지
  - originalException: FirebaseAuthException (디버깅용)
  ↓
state = AsyncValue.error(authException, stackTrace)
  ↓
LoginPage UI
  - authState.when(
      error: (error, stack) {
        if (error is AuthException) {
          return ErrorDialog(message: error.message)
        }
      }
    )
```

---

### 2.1.4 초기 정보 입력 화면 (User Info Input)

#### 화면 구성 (3단계)
```
[Step 1: 생년월일]
┌─────────────────────────┐
│  ← [이전]        1/3     │
│  ━━━━━━━━━━━━━━━━━━━   │ (33%)
│  생년월일을 알려주세요    │
│  더 나은 추천을 위해      │
│  필요해요                │
│  ┌─────────────────┐    │
│  │  1995년 3월 15일 │    │
│  │   [날짜 선택]    │    │
│  └─────────────────┘    │
│        [다음 →]         │
└─────────────────────────┘

[Step 2: 성별]
┌─────────────────────────┐
│  ← [이전]        2/3     │
│  ━━━━━━━━━━━━━━━━━━━   │ (66%)
│  성별을 선택해주세요      │
│  ┌─────────────────┐    │
│  │    👨 남성       │    │
│  └─────────────────┘    │
│  ┌─────────────────┐    │
│  │    👩 여성       │    │
│  └─────────────────┘    │
│  ┌─────────────────┐    │
│  │    🧑 기타       │    │
│  └─────────────────┘    │
│        [다음 →]         │
└─────────────────────────┘

[Step 3: 닉네임]
┌─────────────────────────┐
│  ← [이전]        3/3     │
│  ━━━━━━━━━━━━━━━━━━━   │ (100%)
│  닉네임을 정해주세요      │
│  ┌─────────────────┐    │
│  │  맵시러버        │    │
│  └─────────────────┘    │
│  ✓ 사용 가능한 닉네임    │
│  • 2-12자 이내           │
│  • 특수문자 제외         │
│      [시작하기]          │
└─────────────────────────┘
```

#### 로직 흐름
```
UserInfoInputPage 진입
  ↓
userInfoStepProvider 초기화
  - currentStep = 0 (Step 1: 생년월일)
  - birthDate: DateTime?
  - gender: Gender?
  - nickname: String?
  ↓
[Step 1: 생년월일]
  - DatePicker 바텀 시트 표시
  - 날짜 선택 → birthDate 저장
  - 유효성 검사: 만 14세 이상
  - "다음" 버튼 → currentStep = 1
  ↓
[Step 2: 성별]
  - 선택 카드 3개 (남성/여성/기타)
  - 카드 클릭 → gender 저장
  - "다음" 버튼 → currentStep = 2
  ↓
[Step 3: 닉네임]
  - TextField 입력
  - 실시간 검증 (debounce 500ms)
    - 길이 체크: 2-12자
    - 정규식: ^[가-힣a-zA-Z0-9]+$
    - API 호출: GET /api/members/check-name?name={nickname}
  - 결과 표시
    - ✓ "사용 가능한 닉네임"
    - ✗ "이미 사용 중인 닉네임입니다"
  - "시작하기" 버튼 (유효한 경우만 활성화)
  ↓
API 호출
  - POST /api/members/onboarding/birth-date
  - POST /api/members/onboarding/gender
  - POST /api/members/profile (닉네임 + 프로필 이미지)
  ↓
성공 시
  - SharedPreferences 저장
    - userInfoCompleted = true
  - HomePage로 이동
  ↓
실패 시
  - 에러 다이얼로그 표시
  - 해당 Step으로 복귀
```

#### Provider 구조
```
userInfoStepProvider (StateProvider<int>)
  - currentStep: 0 (생년월일), 1 (성별), 2 (닉네임)

userInfoFormProvider (NotifierProvider<UserInfoFormNotifier, UserInfoForm>)
  ↓
UserInfoFormNotifier
  - state: UserInfoForm (birthDate, gender, nickname)
  - updateBirthDate(DateTime date)
  - updateGender(Gender gender)
  - updateNickname(String nickname)
  - validateNickname() → API 호출 (debounced)
  - submitUserInfo() → 3개 API 호출 (순차)

nicknameValidationProvider (FutureProvider<bool>)
  → API GET /api/members/check-name
  → bool (사용 가능 여부)
```

---

## 2.2 홈 피드 화면

### 2.2.1 화면 구성
```
┌─────────────────────────┐
│ [Logo]         [🔔 알림] │ ← 헤더 (고정)
├─────────────────────────┤
│ ┌─ 섹션 1: 최신 장소 ───┐│ ← 무한 스크롤
│ │  🆕 방금 추가된 장소  ││
│ │ ┌──────────────────┐ ││
│ │ │ [썸네일 16:9]     │ ││
│ │ │ 카페 온더무드      │ ││
│ │ │ 서울 종로구...    │ ││
│ │ │ #성수동 #카페     │ ││
│ │ │ 💾 45명 저장      │ ││
│ │ └──────────────────┘ ││
│ └──────────────────────┘│
│ ┌─ 섹션 2: 인기 피드 ───┐│
│ │  🔥 지금 핫한 장소    ││
│ │ [썸] [썸] [썸]       ││
│ └──────────────────────┘│
│ ┌─ 섹션 3: 내 TOP ──────┐│
│ │  ⭐ 내가 저장한 인기   ││
│ │ [카드] [카드] [카드]  ││
│ └──────────────────────┘│
│ ┌─ 섹션 4: 트렌드 키워드┐│
│ │  📈 요즘 뜨는 키워드  ││
│ │ [#성수동] [#브런치]   ││
│ └──────────────────────┘│
└─────────────────────────┘
         [+ FAB]
```

### 2.2.2 섹션 1: 최신 장소 (무한 스크롤)

#### 로직 흐름
```
HomePage 진입
  ↓
latestPlacesProvider 초기화
  - state = AsyncValue.loading()
  - fetchInitial() 호출
  ↓
fetchInitial()
  - API GET /api/feed/latest/cursor?cursor=null&size=20
  - Response: { content: Place[], cursor: { next: "...", hasNext: true } }
  - state = AsyncValue.data(places)
  - nextCursor 저장
  ↓
ListView.builder 렌더링
  - itemCount: places.length + 1 (로딩 인디케이터)
  - itemBuilder: PlaceCard 위젯
  - 스크롤 감지: ScrollController
  ↓
스크롤 하단 도달 (position >= maxScrollExtent - 200px)
  ↓
fetchMore() 호출
  - hasNext == false → 리턴
  - isLoadingMore == true → 리턴 (중복 호출 방지)
  - isLoadingMore = true
  - API GET /api/feed/latest/cursor?cursor={nextCursor}&size=20
  - Response: { content: Place[], cursor: { next: "...", hasNext: false } }
  - state = AsyncValue.data([...기존 데이터, ...새 데이터])
  - nextCursor 업데이트
  - isLoadingMore = false
  ↓
마지막 페이지 도달
  - hasNext == false
  - "모든 장소를 확인했습니다" 메시지 표시
```

#### Provider 구조
```
latestPlacesProvider (NotifierProvider<LatestPlacesNotifier, AsyncValue<List<Place>>>)
  ↓
LatestPlacesNotifier
  - state: AsyncValue<List<Place>>
  - nextCursor: String?
  - hasNext: bool
  - isLoadingMore: bool

  - fetchInitial()
    → API GET /api/feed/latest/cursor
    → state = data(places)
    → nextCursor 저장

  - fetchMore()
    → API GET /api/feed/latest/cursor?cursor={nextCursor}
    → state = data([...기존, ...새로운])
    → nextCursor 업데이트
    → hasNext 업데이트

  - refresh()
    → 전체 데이터 재로딩
    → nextCursor = null
    → fetchInitial()
```

---

### 2.2.3 섹션 2: 인기 장소 (가중치 알고리즘)

#### 화면 구성
```
┌─ 섹션 2: 인기 인스타 피드 ───┐
│  🔥 지금 핫한 장소            │
│                              │
│  [썸네일] [썸네일] [썸네일]   │ ← 그리드 3열
│   장소1     장소2     장소3    │
│  🔥인기    💾45     ⭐4.5    │
│                              │
│  [썸네일] [썸네일] [썸네일]   │
│   장소4     장소5     장소6    │
└──────────────────────────────┘
```

#### 로직 흐름
```
HomePage 진입
  ↓
popularPlacesProvider 초기화
  - state = AsyncValue.loading()
  - fetchPopularPlaces() 호출
  ↓
fetchPopularPlaces()
  - API GET /api/feed/popular?cursor=null&size=30
  - 백엔드에서 가중치 적용
    - 인기도 점수 = (최근 7일 저장 횟수 × 2) + (7일 이전 저장 횟수 × 1)
    - ORDER BY weightedScore DESC, createdAt DESC
  - Response: { content: Place[], cursor: { next: "...", hasNext: true } }
  - state = AsyncValue.data(places)
  ↓
GridView.builder 렌더링
  - crossAxisCount: 3
  - childAspectRatio: 1:1 (정사각형)
  - itemBuilder: PopularPlaceTile
    - 썸네일 이미지
    - 장소명 (1줄, 오버레이)
    - 배지: 상위 30% → "🔥 인기"
  ↓
가로 스크롤
  - physics: ScrollPhysics.never (부모 ListView와 충돌 방지)
  - shrinkWrap: true
  - 최대 30개까지만 표시
```

#### Provider 구조
```
popularPlacesProvider (FutureProvider<List<Place>>)
  → API GET /api/feed/popular
  → List<Place> 반환
  → 1시간 캐시 (autoDispose: false)
```

---

### 2.2.4 섹션 3: 내 저장 장소 TOP

#### 화면 구성
```
┌─ 섹션 3: 내 저장 장소 TOP ────┐
│  ⭐ 내가 저장한 인기 장소      │
│                              │
│  [카드 1] [카드 2] [카드 3]   │ ← 가로 스크롤
│  ┌─────┐ ┌─────┐ ┌─────┐    │
│  │[썸]  │ │[썸]  │ │[썸]  │   │
│  │ 장소1 │ │ 장소2 │ │ 장소3 │  │
│  │⭐⭐⭐│ │⭐⭐  │ │⭐⭐⭐│  │
│  └─────┘ └─────┘ └─────┘    │
└──────────────────────────────┘

[비로그인 시]
┌─ 섹션 3: 내 저장 장소 TOP ────┐
│  ⭐ 내가 저장한 인기 장소      │
│                              │
│  로그인하고 내 장소를          │
│  저장하세요                   │
│      [로그인하기]             │
└──────────────────────────────┘
```

#### 로직 흐름
```
HomePage 진입
  ↓
authStateProvider 확인
  - User == null → 비로그인 위젯 표시
  - User != null → myTopPlacesProvider 호출
  ↓
myTopPlacesProvider 초기화
  - state = AsyncValue.loading()
  - fetchMyTopPlaces() 호출
  ↓
fetchMyTopPlaces()
  - API GET /api/feed/my-top-places?limit=10
  - 백엔드 로직
    - 내가 저장한 장소 중 saveCount 높은 순
    - 최대 10개
  - Response: List<Place>
  - state = AsyncValue.data(places)
  ↓
ListView.builder (가로 스크롤)
  - scrollDirection: Axis.horizontal
  - itemCount: min(places.length, 10)
  - itemBuilder: MyTopPlaceCard
    - 썸네일 (4:3)
    - 장소명 (1줄)
    - 내 별점 (⭐ 1-5개)
  ↓
카드 클릭
  - PlaceDetailPage로 이동 (placeId 전달)
```

#### Provider 구조
```
myTopPlacesProvider (FutureProvider<List<Place>>)
  → authStateProvider 의존
  → User == null → []
  → User != null → API GET /api/feed/my-top-places
  → List<Place> 반환
```

---

### 2.2.5 섹션 4: 트렌드 키워드

#### 화면 구성
```
┌─ 섹션 4: 떠오르는 키워드 트렌드 ─┐
│  📈 요즘 뜨는 키워드             │
│                                │
│  [#성수동카페 +120%] [#브런치]  │ ← 칩 형태, 2줄
│  [#감성카페] [#데이트 코스]     │
│  [#부산여행] [#강남맛집]        │
│                                │
│  각 칩: 키워드명 + 상승률 + 개수 │
└────────────────────────────────┘
```

#### 로직 흐름
```
HomePage 진입
  ↓
trendingKeywordsProvider 초기화
  - state = AsyncValue.loading()
  - fetchTrendingKeywords() 호출
  ↓
fetchTrendingKeywords()
  - API GET /api/feed/trending-keywords?limit=20&days=7
  - 백엔드 로직
    - 최근 7일 기준 trendScore 상위 20개
    - trendScore 계산: (최근 사용 빈도 / 이전 사용 빈도) × 100
  - Response: List<Keyword>
    - keyword: String
    - count: int (관련 장소 개수)
    - trendScore: double
    - changeRate: String ("+120%")
  - state = AsyncValue.data(keywords)
  ↓
Wrap 위젯 렌더링 (자동 줄바꿈)
  - spacing: 8.0
  - runSpacing: 8.0
  - children: KeywordChip[]
  ↓
KeywordChip 구성
  - 배경색: trendScore > 100 → 빨간색 (상승), < 100 → 회색
  - 텍스트: "#${keyword}"
  - 상승률: changeRate (작은 글씨, 빨간색)
  - 개수: "${count}개" (회색)
  ↓
칩 클릭
  - KeywordFeedPage로 이동 (keyword 전달)
```

#### Provider 구조
```
trendingKeywordsProvider (FutureProvider<List<Keyword>>)
  → API GET /api/feed/trending-keywords
  → List<Keyword> 반환
  → Redis 캐시 (1시간 TTL)
  → autoDispose: false (세션 유지)
```

---

### 2.2.6 플로팅 액션 버튼 (FAB)

#### 화면 구성
```
┌─────────────────────────┐
│                         │
│    [홈 피드 콘텐츠]      │
│                         │
│                         │
│                    [+]  │ ← 우측 하단 FAB
└─────────────────────────┘
```

#### 로직 흐름
```
FAB 클릭
  ↓
aiExtractionBottomSheet 표시
  - showModalBottomSheet()
  - builder: AiExtractionBottomSheet 위젯
  ↓
URL 입력 바텀 시트
  - TextField (URL 입력)
  - 지원 플랫폼 안내
    - ✅ Instagram 릴스/게시물
    - ✅ YouTube 영상/쇼츠
  - "추출 시작" 버튼
  ↓
"추출 시작" 버튼 클릭
  - URL 유효성 검사
    - 정규식: Instagram/YouTube 패턴 매칭
  - aiExtractionProvider.submitUrl(url) 호출
  ↓
바텀 시트 닫기
  - Navigator.pop(context)
  ↓
처리 중 화면으로 자동 전환
  - AiProcessingPage로 이동
```

---

## 2.3 AI 추출 플로우

### 2.3.1 URL 입력 바텀 시트

#### 화면 구성
```
┌─────────────────────────┐
│  장소 추출하기           │
│  Instagram 또는 YouTube │
│  URL을 붙여넣으세요      │
│  ┌─────────────────┐    │
│  │ https://insta... │    │
│  └─────────────────┘    │
│  ✅ Instagram 릴스/게시물│
│  ✅ YouTube 영상/쇼츠    │
│      [추출 시작]         │
└─────────────────────────┘
```

#### 로직 흐름
```
TextField 입력
  ↓
실시간 유효성 검사
  - onChanged: (value) → urlValidationProvider.validate(value)
  - 정규식 패턴
    - Instagram: ^https?://(www\.)?instagram\.com/(p|reel)/
    - YouTube: ^https?://(www\.)?(youtube\.com|youtu\.be)/
  - 결과: bool isValid
  ↓
"추출 시작" 버튼 활성화
  - isValid == true → enabled
  - isValid == false → disabled
  ↓
버튼 클릭
  ↓
aiExtractionProvider.submitUrl(url)
  - state = AsyncValue.loading()
  - API POST /api/content/analyze
    - Request: { url: "...", platform: "INSTAGRAM" }
    - Response: { contentId: "uuid", status: "PENDING" }
  - state = AsyncValue.data(ExtractionResult)
  - contentId 저장
  ↓
바텀 시트 닫기
  ↓
AiProcessingPage로 이동
  - context.push('/ai-processing/${contentId}')
```

#### Provider 구조
```
urlValidationProvider (StateProvider<bool>)
  - isValid: bool
  - validate(String url) → 정규식 검사

aiExtractionProvider (NotifierProvider<AiExtractionNotifier, AsyncValue<ExtractionResult>>)
  ↓
AiExtractionNotifier
  - state: AsyncValue<ExtractionResult>
  - contentId: String?
  - status: ExtractionStatus

  - submitUrl(String url)
    → API POST /api/content/analyze
    → contentId 저장
    → startPolling() 호출

  - startPolling()
    → Timer.periodic(Duration(seconds: 2))
    → pollStatus() 호출

  - pollStatus()
    → API GET /api/content/{contentId}
    → status 확인
      - PENDING/PROCESSING → 계속 폴링
      - COMPLETED → state = data(result), 타이머 중지
      - FAILED → state = error, 타이머 중지
```

---

### 2.3.2 처리 중 화면 (Polling)

#### 화면 구성
```
┌─────────────────────────┐
│                         │
│    [Lottie 애니메이션]   │
│                         │
│   콘텐츠 다운로드 중...   │
│                         │
│   보통 30초 정도 걸려요   │
│                         │
│       [취소]            │
└─────────────────────────┘
```

#### 로직 흐름
```
AiProcessingPage 진입
  ↓
aiExtractionProvider 확인
  - contentId 존재 확인
  - contentId == null → 에러 화면
  ↓
startPolling() 자동 시작
  - Timer.periodic(Duration(seconds: 2))
  - pollStatus() 호출
  ↓
pollStatus() 반복
  - API GET /api/content/{contentId}
  - Response: { status: "PROCESSING", progress: "영상 분석 중..." }
  - 진행 상태 텍스트 업데이트
    - "콘텐츠 다운로드 중..."
    - "영상 분석 중..."
    - "장소 추출 중..."
  ↓
상태 변화 감지
  ↓
[COMPLETED]
  - state = AsyncValue.data(result)
  - Timer 중지
  - AiResultPage로 자동 전환
  ↓
[FAILED]
  - state = AsyncValue.error(error)
  - Timer 중지
  - AiErrorPage로 자동 전환
  ↓
[취소 버튼 클릭]
  - Timer 중지
  - Navigator.pop()
  - 홈으로 복귀
```

#### Provider 구조
```
aiExtractionProvider (NotifierProvider)
  ↓
AiExtractionNotifier
  - pollingTimer: Timer?
  - progressText: String

  - startPolling()
    → pollingTimer = Timer.periodic(2초)
    → 최대 60초 (30회) 제한

  - pollStatus()
    → API GET /api/content/{contentId}
    → status 확인 및 상태 업데이트

  - cancelPolling()
    → pollingTimer?.cancel()
    → state = AsyncValue.data(null)
```

---

### 2.3.3 추출 완료 화면

#### 화면 구성
```
┌─────────────────────────┐
│  ✅ 3개의 장소를 찾았어요!│
│                         │
│ ┌──────────────────┐    │
│ │ ☑ [썸] 카페 온더무드 │
│ │      서울 종로구   │
│ │      "감성 카페...\"│
│ └──────────────────┘    │
│ ┌──────────────────┐    │
│ │ ☑ [썸] 장소명2    │
│ │      주소...      │
│ └──────────────────┘    │
│ ┌──────────────────┐    │
│ │ ☐ [썸] 장소명3    │
│ │      주소...      │
│ └──────────────────┘    │
│     [3개 저장하기]       │
│     [건너뛰기]          │
└─────────────────────────┘
```

#### 로직 흐름
```
AiResultPage 진입
  ↓
aiExtractionProvider.state 확인
  - AsyncValue.data(ExtractionResult)
  - result.places: List<Place>
  ↓
selectedPlacesProvider 초기화
  - Set<String> (선택된 placeId 집합)
  - 초기값: 모든 placeId (전체 선택)
  ↓
체크박스 토글
  - onChanged: (bool? checked)
  - checked == true → selectedPlaces.add(placeId)
  - checked == false → selectedPlaces.remove(placeId)
  - selectedCount 업데이트
  ↓
"N개 저장하기" 버튼 클릭
  ↓
FolderSelectionBottomSheet 표시
  - showModalBottomSheet()
  - 폴더 목록 표시
    - 가고 싶은 곳
    - 가본 곳
    - 커스텀 폴더 (사용자 생성)
  ↓
폴더 선택
  ↓
savePlaces() 호출
  - selectedPlaces.forEach((placeId) {
      API POST /api/bookmarks/{placeId}/save
      Request: { folder: "가고 싶은 곳", savedStatus: "SAVED" }
    })
  - 병렬 처리 (Future.wait)
  ↓
성공 시
  - 토스트 메시지: "3개 장소가 저장되었습니다"
  - BookmarkPage로 이동 (해당 폴더 탭)
  ↓
실패 시
  - 에러 다이얼로그
  - "일부 장소 저장에 실패했습니다 (2/3 성공)"
```

#### Provider 구조
```
selectedPlacesProvider (StateProvider<Set<String>>)
  - selectedPlaceIds: Set<String>
  - toggleSelection(String placeId)

savePlacesProvider (FutureProvider)
  - savePlaces(Set<String> placeIds, String folder)
  → Future.wait([
      API POST /api/bookmarks/{id1}/save,
      API POST /api/bookmarks/{id2}/save,
      ...
    ])
  → SaveResult (successCount, failedCount)
```

---

### 2.3.4 추출 실패 화면

#### 화면 구성
```
┌─────────────────────────┐
│      [에러 아이콘]       │
│   장소를 찾을 수 없어요   │
│  영상에 장소 정보가 없거나│
│  분석에 실패했어요        │
│      [다시 시도]         │
│    [수동으로 추가]       │
└─────────────────────────┘
```

#### 로직 흐름
```
AiErrorPage 진입
  ↓
aiExtractionProvider.state 확인
  - AsyncValue.error(error)
  - error.message 표시
  ↓
에러 타입별 메시지
  - NETWORK_ERROR → "네트워크 오류가 발생했습니다"
  - UNSUPPORTED_PLATFORM → "지원하지 않는 플랫폼입니다"
  - NO_PLACES_FOUND → "장소를 찾을 수 없어요"
  - AI_PROCESSING_FAILED → "분석에 실패했어요"
  ↓
"다시 시도" 버튼 클릭
  - aiExtractionProvider.reset()
  - URL 입력 바텀 시트로 복귀
  ↓
"수동으로 추가" 버튼 클릭
  - ManualPlaceAddPage로 이동
  - 장소명/주소 직접 입력
  - Kakao Local API 검색
```

---

## 2.4 장소 상세 화면

### 2.4.1 화면 구성
```
┌─────────────────────────┐
│ ← [대표 이미지 16:9] 🔗 │
│     카페 온더무드        │
├─────────────────────────┤
│ 🍰 카페                 │
│ ⭐ 4.5 (리뷰 120개)     │
│ 📍 서울 종로구 삼청로... │
│ 📞 02-1234-5678        │
├─────────────────────────┤
│ ⏰ 영업 시간             │
│ 🟢 영업 중              │
│ 오늘 10:00 - 22:00     │
│ [▼ 더보기]              │
├─────────────────────────┤
│ 📍 위치 정보            │
│ ┌─────────────────┐    │
│ │   [Map View]    │    │
│ └─────────────────┘    │
│ [카카오맵] [네이버맵]    │
│ [Google Maps]          │
├─────────────────────────┤
│ 🏷️ 관련 키워드          │
│ [#성수동] [#카페] [#브런치]│
├─────────────────────────┤
│ 📷 사진 (12개)          │
│ [썸] [썸] [썸]          │
│      [더보기]           │
├─────────────────────────┤
│ 📱 이 장소를 언급한 게시물│
│ ┌──────┐ ┌──────┐      │
│ │[썸네일]│ │[썸네일]│   │
│ │IG 맛집 │ │YT 여행 │   │
│ └──────┘ └──────┘      │
└─────────────────────────┘
│ [💾 저장하기]   [🔗 공유]│
└─────────────────────────┘
```

### 2.4.2 로직 흐름
```
PlaceDetailPage 진입
  - placeId 파라미터 전달
  ↓
placeDetailProvider(placeId) 호출
  - state = AsyncValue.loading()
  - fetchPlaceDetail(placeId) 호출
  ↓
fetchPlaceDetail()
  - API GET /api/place/{placeId}
  - Response: PlaceDetailResponse
    - basicInfo: { name, category, rating, address, phone }
    - businessHours: { status, todayHours, weeklyHours[] }
    - location: { latitude, longitude }
    - keywords: Keyword[]
    - photos: String[] (이미지 URL 배열)
    - sourceContents: Content[] (출처 콘텐츠)
  - state = AsyncValue.data(placeDetail)
  ↓
화면 렌더링
  ↓
[헤더 이미지]
  - CachedNetworkImage (photos[0])
  - 뒤로가기 버튼 (좌측 상단)
  - 공유 버튼 (우측 상단)
  ↓
[기본 정보]
  - 카테고리 아이콘 + 텍스트
  - 평점 (⭐ + 숫자 + 리뷰 개수)
  - 주소 (클릭 시 클립보드 복사)
  - 전화번호 (클릭 시 url_launcher)
  ↓
[영업 정보]
  - 현재 상태: businessStatus
    - OPEN → 🟢 영업 중 (초록색)
    - CLOSED → 🔴 영업 종료 (빨간색)
  - 오늘 영업시간: todayHours
  - "더보기" 버튼 클릭 → isExpanded = true
    - 요일별 영업시간 표시 (아코디언)
  ↓
[위치 정보]
  - GoogleMap 위젯 (축소)
  - initialCameraPosition: LatLng(latitude, longitude)
  - markers: [Marker(placeId)]
  - 지도 클릭 → FullMapPage로 이동
  - 길찾기 버튼
    - 카카오맵: kakaomap://look?p={lat},{lng}
    - 네이버맵: nmap://place?lat={lat}&lng={lng}
    - 구글맵: https://maps.google.com/?q={lat},{lng}
  ↓
[관련 키워드]
  - Wrap 위젯 (keywords)
  - KeywordChip 클릭 → KeywordFeedPage
  ↓
[사진 갤러리]
  - GridView (3열, 최대 9개)
  - "더보기" 버튼 (10개 이상 시)
  - 썸네일 클릭 → PhotoGalleryPage (전체 화면)
  ↓
[출처 콘텐츠]
  - ListView (가로 스크롤)
  - ContentCard (썸네일 + 플랫폼 아이콘 + 게시자)
  - 카드 클릭 → url_launcher (외부 브라우저)
  ↓
[하단 고정 버튼]
  - "💾 저장하기" 버튼
  - isSavedProvider 확인
    - false → FolderSelectionBottomSheet
    - true → "✓ 저장됨" (비활성화)
  - "🔗 공유" 버튼
    - Share.share("장소명 - 주소 - URL")
```

### 2.4.3 Provider 구조
```
placeDetailProvider(String placeId) (FutureProvider<PlaceDetail>)
  → API GET /api/place/{placeId}
  → PlaceDetail 반환
  → family 패턴 사용 (placeId별 캐싱)

isSavedProvider(String placeId) (FutureProvider<bool>)
  → API GET /api/bookmarks?placeId={placeId}
  → bool 반환 (저장 여부)

businessHoursExpandedProvider (StateProvider<bool>)
  → isExpanded: bool (영업시간 더보기 상태)
```

---

## 2.5 북마크 관리 화면

### 2.5.1 화면 구성
```
┌─────────────────────────┐
│ ☰ 내 저장 장소  [🔍][⚙] │
├─────────────────────────┤
│ [전체] [가고싶은곳] [가본곳]│ ← 탭 바
│   15      8        3     │
├─────────────────────────┤
│ [최근순▼] [방문함 ☐]     │ ← 정렬/필터
├─────────────────────────┤
│ ┌──────────────────┐    │
│ │ [썸] 카페 온더무드 [⋮]│
│ │      서울 종로구   ⭐⭐⭐│
│ │      "데이트 코스" ✓방문│
│ └──────────────────┘    │
│ ┌──────────────────┐    │
│ │ [썸] 장소명       [⋮]│
│ │      주소          ⭐⭐│
│ │      "메모...\"        │
│ └──────────────────┘    │
│     ... (무한 스크롤)    │
└─────────────────────────┘
```

### 2.5.2 로직 흐름
```
BookmarkPage 진입
  ↓
bookmarkFoldersProvider 호출
  - API GET /api/bookmarks/folders
  - Response: Folder[]
    - { id: "전체", name: "전체", count: 15 }
    - { id: "want", name: "가고 싶은 곳", count: 8 }
    - { id: "visited", name: "가본 곳", count: 3 }
    - 커스텀 폴더[] (사용자 생성)
  ↓
TabController 초기화
  - tabs: folders.map((f) => Tab(text: "${f.name} (${f.count})"))
  - initialIndex: 0 (전체)
  ↓
currentFolderProvider 업데이트
  - 현재 선택된 폴더 ID
  ↓
bookmarkedPlacesProvider(folderId) 호출
  - state = AsyncValue.loading()
  - fetchBookmarkedPlaces(folderId) 호출
  ↓
fetchBookmarkedPlaces()
  - API GET /api/bookmarks?folder={folderId}&page=0&size=20&sort=createdAt,DESC
  - Response: PageResponse<MemberPlace>
    - content: MemberPlace[]
    - page: { number, size, totalElements, totalPages, hasNext }
  - state = AsyncValue.data(places)
  ↓
ListView.builder 렌더링
  - itemCount: places.length
  - itemBuilder: BookmarkedPlaceCard
  ↓
[정렬 드롭다운 변경]
  - sortOptionProvider.update("rating,DESC")
  - bookmarkedPlacesProvider.refresh()
  ↓
[필터 토글 변경]
  - visitedFilterProvider.update(true)
  - API 재호출 (visited=true 파라미터 추가)
  ↓
[스와이프 액션]
  - 좌측 스와이프 → 삭제 버튼 (빨간색)
    - confirmDelete() 다이얼로그
    - API DELETE /api/bookmarks/{bookmarkId}
    - Soft Delete (deletedAt 기록)
  - 우측 스와이프 → 메모 편집 버튼 (파란색)
    - MemoEditBottomSheet 표시
  ↓
[메뉴 아이콘 (⋮) 클릭]
  - BookmarkMenuBottomSheet 표시
    - "메모 수정"
    - "별점 변경"
    - "방문 날짜 기록"
    - "폴더 이동"
    - "삭제" (빨간색)
  ↓
각 메뉴 액션
  - 메모 수정 → MemoEditBottomSheet
  - 별점 변경 → RatingDialog (1-5 별점)
  - 방문 날짜 → DatePicker → API PATCH
  - 폴더 이동 → FolderSelectionBottomSheet
  - 삭제 → confirmDelete()
```

### 2.5.3 Provider 구조
```
bookmarkFoldersProvider (FutureProvider<List<Folder>>)
  → API GET /api/bookmarks/folders
  → List<Folder> 반환

currentFolderProvider (StateProvider<String>)
  → currentFolderId: String (선택된 폴더 ID)

sortOptionProvider (StateProvider<String>)
  → sortBy: "createdAt,DESC" | "name,ASC" | "rating,DESC" | "visitedAt,DESC"

visitedFilterProvider (StateProvider<bool?>)
  → visited: null (전체) | true (방문함) | false (미방문)

bookmarkedPlacesProvider(folderId) (NotifierProvider)
  ↓
BookmarkedPlacesNotifier
  - state: AsyncValue<PageResponse<MemberPlace>>
  - currentPage: int
  - hasNext: bool

  - fetchBookmarkedPlaces(folderId, page, sort, visited)
    → API GET /api/bookmarks
    → state = data(response)

  - fetchMore()
    → currentPage++
    → API 호출
    → state = data([...기존, ...새로운])

  - deleteBookmark(bookmarkId)
    → API DELETE /api/bookmarks/{bookmarkId}
    → state에서 해당 항목 제거

  - updateMemo(bookmarkId, memo)
    → API PATCH /api/bookmarks/{bookmarkId}
    → state 업데이트
```

---

## 2.6 디자인 시스템

### 2.6.1 컬러 팔레트

#### 브랜드 컬러
```dart
// lib/core/constants/app_colors.dart

class AppColors {
  // Primary (주요 액션)
  static const primary500 = Color(0xFFFF6B6B);
  static const primary400 = Color(0xFFFF8787);
  static const primary600 = Color(0xFFFF4E4E);

  // Secondary (보조 액션)
  static const secondary500 = Color(0xFF4ECDC4);
  static const secondary400 = Color(0xFF72D7D0);
  static const secondary600 = Color(0xFF2BB8AE);

  // Accent (강조)
  static const accent500 = Color(0xFFFFD93D);

  // Background
  static const backgroundPrimary = Color(0xFFFFFFFF);
  static const backgroundSecondary = Color(0xFFF8F9FA);
  static const backgroundTertiary = Color(0xFFE9ECEF);

  // Text
  static const textPrimary = Color(0xFF212529);
  static const textSecondary = Color(0xFF6C757D);
  static const textTertiary = Color(0xFFADB5BD);
  static const textInverse = Color(0xFFFFFFFF);

  // Border
  static const borderLight = Color(0xFFDEE2E6);
  static const borderDefault = Color(0xFFCED4DA);
  static const borderDark = Color(0xFFADB5BD);

  // Status
  static const success = Color(0xFF51CF66);
  static const warning = Color(0xFFFFA94D);
  static const error = Color(0xFFFF6B6B);
  static const info = Color(0xFF4DABF7);
}
```

### 2.6.2 타이포그래피

```dart
// lib/core/constants/text_styles.dart

class AppTextStyles {
  // Heading
  static const h1 = TextStyle(
    fontFamily: 'Pretendard-Bold',
    fontSize: 28,
    fontWeight: FontWeight.w700,
    height: 1.3,
  );

  static const h2 = TextStyle(
    fontFamily: 'Pretendard-Bold',
    fontSize: 24,
    fontWeight: FontWeight.w700,
    height: 1.3,
  );

  static const h3 = TextStyle(
    fontFamily: 'Pretendard-SemiBold',
    fontSize: 20,
    fontWeight: FontWeight.w600,
    height: 1.4,
  );

  static const h4 = TextStyle(
    fontFamily: 'Pretendard-SemiBold',
    fontSize: 18,
    fontWeight: FontWeight.w600,
    height: 1.4,
  );

  // Body
  static const body1 = TextStyle(
    fontFamily: 'Pretendard-Regular',
    fontSize: 16,
    fontWeight: FontWeight.w400,
    height: 1.5,
  );

  static const body2 = TextStyle(
    fontFamily: 'Pretendard-Regular',
    fontSize: 14,
    fontWeight: FontWeight.w400,
    height: 1.5,
  );

  static const body3 = TextStyle(
    fontFamily: 'Pretendard-Regular',
    fontSize: 12,
    fontWeight: FontWeight.w400,
    height: 1.5,
  );

  // Button
  static const buttonLarge = TextStyle(
    fontFamily: 'Pretendard-SemiBold',
    fontSize: 16,
    fontWeight: FontWeight.w600,
  );

  static const buttonMedium = TextStyle(
    fontFamily: 'Pretendard-SemiBold',
    fontSize: 14,
    fontWeight: FontWeight.w600,
  );

  static const buttonSmall = TextStyle(
    fontFamily: 'Pretendard-SemiBold',
    fontSize: 12,
    fontWeight: FontWeight.w600,
  );

  // Caption
  static const caption1 = TextStyle(
    fontFamily: 'Pretendard-Regular',
    fontSize: 12,
    fontWeight: FontWeight.w400,
    height: 1.4,
  );

  static const caption2 = TextStyle(
    fontFamily: 'Pretendard-Regular',
    fontSize: 10,
    fontWeight: FontWeight.w400,
    height: 1.4,
  );
}
```

### 2.6.3 간격 및 레이아웃

```dart
// lib/core/constants/spacing_and_radius.dart

class AppSpacing {
  // Vertical Spacing
  static const vertical4 = 4.0.h;
  static const vertical8 = 8.0.h;
  static const vertical12 = 12.0.h;
  static const vertical16 = 16.0.h;
  static const vertical24 = 24.0.h;
  static const vertical32 = 32.0.h;

  // Horizontal Spacing
  static const horizontal4 = 4.0.w;
  static const horizontal8 = 8.0.w;
  static const horizontal12 = 12.0.w;
  static const horizontal16 = 16.0.w;
  static const horizontal24 = 24.0.w;
  static const horizontal32 = 32.0.w;
}

class AppPadding {
  // All Padding
  static EdgeInsets all4 = EdgeInsets.all(4.r);
  static EdgeInsets all8 = EdgeInsets.all(8.r);
  static EdgeInsets all12 = EdgeInsets.all(12.r);
  static EdgeInsets all16 = EdgeInsets.all(16.r);
  static EdgeInsets all20 = EdgeInsets.all(20.r);
  static EdgeInsets all24 = EdgeInsets.all(24.r);

  // Horizontal/Vertical Padding
  static EdgeInsets horizontal16 = EdgeInsets.symmetric(horizontal: 16.w);
  static EdgeInsets vertical16 = EdgeInsets.symmetric(vertical: 16.h);
}

class AppRadius {
  // Border Radius
  static BorderRadius radius4 = BorderRadius.circular(4.r);
  static BorderRadius radius8 = BorderRadius.circular(8.r);
  static BorderRadius radius12 = BorderRadius.circular(12.r);
  static BorderRadius radius16 = BorderRadius.circular(16.r);
  static BorderRadius radius50 = BorderRadius.circular(50.r);
}
```

---

# Part 3: 백엔드 API 간결 정리

## 3.1 API 엔드포인트 목록

### 인증 API
```
POST   /api/auth/sign-in          Firebase OAuth 로그인
POST   /api/auth/reissue           JWT 토큰 갱신
POST   /api/auth/logout            로그아웃
DELETE /api/auth/withdraw          회원 탈퇴
```

### 회원 관리 API
```
POST /api/members/onboarding/terms       약관 동의
POST /api/members/onboarding/birth-date  생년월일 입력
POST /api/members/onboarding/gender      성별 선택
POST /api/members/profile                프로필 수정
GET  /api/members/check-name             닉네임 중복 검사
GET  /api/members/{memberId}             회원 정보 조회
```

### 피드 API
```
GET /api/feed/latest/cursor      최신 장소 피드 (Cursor)
GET /api/feed/popular            인기 장소 피드 (가중치)
GET /api/feed/my-top-places      내 저장 장소 TOP
GET /api/feed/trending-keywords  트렌드 키워드
```

### 키워드 API
```
GET /api/keywords/{keyword}         키워드 상세 정보
GET /api/keywords/{keyword}/places  키워드별 장소 목록
```

### 장소 관리 API
```
GET    /api/place/{placeId}              장소 상세 정보
GET    /api/place/temporary              임시 저장 장소
POST   /api/place/{placeId}/save         장소 저장
DELETE /api/place/{placeId}/temporary    임시 저장 삭제
```

### 북마크 API
```
GET   /api/bookmarks               저장 장소 목록 (Offset)
GET   /api/bookmarks/cursor        저장 장소 목록 (Cursor)
GET   /api/bookmarks/folders       폴더 목록
PATCH /api/bookmarks/{bookmarkId}  북마크 정보 수정
DELETE /api/bookmarks/{bookmarkId} 북마크 삭제 (Soft Delete)
```

### 콘텐츠/AI 추출 API
```
POST /api/content/analyze     SNS URL 장소 추출 요청
GET  /api/content/{contentId} 콘텐츠 정보 조회 (폴링용)
GET  /api/content/member      내 콘텐츠 목록
```

---

## 3.2 페이지네이션 전략

### Cursor 기반 (실시간 피드)
```
사용 케이스:
- 홈 피드 (최신 장소)
- 홈 피드 (인기 장소)

Request:
  cursor: String? (Base64 encoded)
  size: Integer (default: 20, max: 50)

Response:
  content: T[]
  cursor: {
    next: String? (다음 커서)
    hasNext: Boolean
  }

로직:
  cursor == null → 첫 페이지
  cursor != null → 디코딩하여 WHERE 절 적용

예시 (최신 장소):
  SELECT * FROM place
  WHERE (created_at, id) < ('{timestamp}', '{uuid}')
  ORDER BY created_at DESC, id DESC
  LIMIT 21
```

### Offset 기반 (정적 목록)
```
사용 케이스:
- 북마크 목록
- 키워드 피드

Request:
  page: Integer (default: 0)
  size: Integer (default: 20)
  sort: String (예: "createdAt,DESC")

Response:
  content: T[]
  page: {
    number: Integer
    size: Integer
    totalElements: Long
    totalPages: Integer
    hasNext: Boolean
  }
  sort: {
    orderBy: String
    direction: "ASC" | "DESC"
  }

로직:
  OFFSET = page * size
  LIMIT = size

예시 (북마크):
  SELECT * FROM member_place
  WHERE member_id = '{uuid}' AND deleted_at IS NULL
  ORDER BY created_at DESC
  LIMIT 20 OFFSET 0
```

---

## 3.3 인증 플로우

### Firebase OAuth → JWT 발급
```
[클라이언트]
  Google/Apple 로그인
    ↓
  Firebase ID Token 획득
    ↓
  POST /api/auth/sign-in
  Request: { idToken: "..." }
    ↓
[백엔드]
  Firebase Admin SDK로 ID Token 검증
    ↓
  사용자 정보 추출 (uid, email, name)
    ↓
  Member 존재 여부 확인
    - 신규 → Member 생성
    - 기존 → Member 조회
    ↓
  JWT Access Token 발급 (1시간)
  JWT Refresh Token 발급 (7일)
    ↓
  Redis에 Refresh Token 저장
  Key: "RT:{memberId}"
  Value: "{refreshToken}"
  TTL: 7일
    ↓
  Response: {
    accessToken: "...",
    refreshToken: "...",
    member: { id, email, name }
  }
    ↓
[클라이언트]
  flutter_secure_storage에 저장
  - accessToken
  - refreshToken
    ↓
  이후 모든 API 요청
  Header: Authorization: Bearer {accessToken}
```

### Token 갱신
```
[클라이언트]
  API 호출 → 401 Unauthorized
    ↓
  POST /api/auth/reissue
  Request: { refreshToken: "..." }
    ↓
[백엔드]
  Refresh Token 검증
    ↓
  Redis에서 확인
  Key: "RT:{memberId}"
    ↓
  일치하면 새 Access Token 발급
    ↓
  Response: {
    accessToken: "...",
    refreshToken: "..." (갱신 또는 재사용)
  }
    ↓
[클라이언트]
  새 Access Token 저장
  원래 API 재시도
```

---

# Part 4: 확장 계획

## 4.1 Phase 3-5 개요

### Phase 3: 소셜 기능 (MVP 출시 후 3개월)
```
컬렉션 공유
  - 폴더별 장소 목록 공유
  - 공유 링크 생성
  - 타인의 컬렉션 구독

팔로우 시스템
  - 사용자 팔로우/언팔로우
  - 팔로잉 피드 (팔로우한 사용자의 저장 장소)

좋아요/댓글
  - 장소에 좋아요 (공감)
  - 댓글 작성 및 답글

알림
  - 팔로워 알림
  - 댓글/좋아요 알림
  - 트렌드 장소 추천 알림
```

### Phase 4: 플랫폼 확장 (MVP 출시 후 6개월)
```
지원 플랫폼 추가
  - TikTok (쇼츠)
  - Facebook (동영상/게시물)
  - Twitter/X (트윗 + 동영상)

AI 정확도 향상
  - Instagram: 85% → 92%
  - YouTube: 80% → 88%
  - TikTok: 목표 85%

자동 좌표 추출
  - LLM이 직접 좌표 추론
  - Geocoding API 의존도 감소
```

### Phase 5: 수익화 (MVP 출시 후 9개월)
```
프리미엄 구독 (월 9,900원)
  - 무제한 AI 추출 (무료: 월 20회)
  - 광고 제거
  - 우선 AI 처리 (대기 시간 단축)
  - 프리미엄 배지

B2B API 제공
  - 마케팅 업체용 API
  - 트렌드 장소 데이터 제공
  - 키워드 분석 리포트

제휴 마케팅
  - 배달 앱 제휴 (쿠폰 제공)
  - 예약 플랫폼 연동 (수수료)
```

---

## 4.2 향후 로드맵

### 2026 Q2 (MVP 출시)
```
✅ Phase 1 완료
- Instagram/YouTube 장소 추출
- 홈 피드 (최신/인기/트렌드)
- 북마크 관리
- App Store/Play Store 출시
```

### 2026 Q3 (기능 확장)
```
🔄 Phase 2 진행
- 컬렉션 공유
- 팔로우/좋아요/댓글
- 푸시 알림
```

### 2026 Q4 (플랫폼 확장)
```
🔄 Phase 3 진행
- TikTok/Facebook/Twitter 지원
- AI 정확도 향상
- 자동 좌표 추출
```

### 2027 Q1 (수익화)
```
🔄 Phase 4 진행
- 프리미엄 구독 출시
- B2B API 제공
- 제휴 마케팅 시작
```

---

# 부록

## 부록 A: 용어 정리

**AI 추출**: SNS 콘텐츠에서 장소 정보를 자동으로 추출하는 기능
**Cursor 페이지네이션**: 커서 기반 무한 스크롤 방식
**Offset 페이지네이션**: 페이지 번호 기반 페이지네이션
**Riverpod**: Flutter 상태 관리 라이브러리 (Provider 진화형)
**Provider 코드 생성**: @riverpod 어노테이션 + build_runner로 자동 생성
**Soft Delete**: 실제 삭제 대신 deletedAt 필드 기록 (30일 복구 가능)
**가중치 알고리즘**: 최근 7일 저장 = 2배, 이전 = 1배로 인기도 계산
**폴링**: 2초 간격으로 API 호출하여 상태 확인

## 부록 B: 외부 API 종속성

**Firebase**:
- Firebase Auth (Google/Apple 소셜 로그인)
- Firebase Messaging (FCM 푸시 알림)
- Firebase Crashlytics (에러 리포팅)

**Google**:
- Gemini 2.5 Flash (LLM)
- YouTube Data API v3
- Google Maps API

**Kakao**:
- Kakao Local API (Geocoding)

**기타**:
- Nominatim (Fallback Geocoding, 무료)
- Tesseract OCR (오픈소스)

## 부록 C: 참고 문서

- Flutter 공식 문서: https://docs.flutter.dev
- Riverpod 공식 문서: https://riverpod.dev
- Firebase 공식 문서: https://firebase.google.com/docs
- Spring Boot 공식 문서: https://spring.io/projects/spring-boot

---

**문서 버전**: v1.0
**마지막 업데이트**: 2026-01-20
**작성자**: Claude (Anthropic AI)
**승인자**: (TBD)

---

**End of Integrated PRD**
