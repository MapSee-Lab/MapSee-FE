# Mapsy PRD 기반 구현 가이드 보고서

**보고서 일자**: 2026-01-19
**참조 문서**: `C:\Users\USER\.claude\plans\misty-growing-wand.md`
**대상**: 개발팀 (Backend, AI, Frontend)
**목적**: PRD 기반 실행 가능한 구현 계획 수립

---

## 📌 개요

Mapsy PRD Part 1 + Part 2를 기반으로 **실제 구현 가능한 단계별 실행 계획** 작성. 각 팀(Backend, AI, Frontend)별 작업 항목을 우선순위와 함께 정리하여 즉시 개발 착수 가능하도록 가이드 제공.

---

## 🎯 구현 목표 및 우선순위

### Phase 1: MVP 개발 (Week 1-12)
**목표**: 핵심 기능 구현 및 베타 테스트 준비

| 우선순위 | 작업 항목 | 담당 | 예상 기간 | 상태 |
|---------|----------|------|----------|------|
| 🔴 P0 | Backend API 완성 | Backend Team | Week 1-4 | 🟡 70% |
| 🔴 P0 | AI 서버 안정화 | AI Team | Week 2-5 | 🟡 60% |
| 🔴 P0 | Flutter 앱 개발 | Frontend Team | Week 3-8 | 🔴 0% |
| 🟡 P1 | 테스트 작성 | All Teams | Week 6-9 | 🔴 0% |
| 🟡 P1 | 베타 테스트 | QA Team | Week 9-10 | 🔴 0% |
| 🟢 P2 | 문서화 | All Teams | Week 10-11 | 🔴 0% |
| 🟢 P2 | 1차 MVP 출시 | DevOps Team | Week 12 | 🔴 0% |

---

## 🖥️ Backend API 구현 가이드

### 현재 상태 (70% 완료)

**✅ 완료된 항목**:
- Spring Boot 4.0.1 Multi-Module 구조 (7개 모듈)
- Firebase OAuth + JWT 인증 시스템
- Member, Place, MemberPlace 기본 CRUD
- Keyword 시스템 (Interest 제거 후 전환)
- AI Callback Webhook (`/api/ai/callback`)
- Database 마이그레이션 (Flyway v0.3.x)

**❌ 미완성 항목**:
- 피드 API 페이지네이션 (Cursor/Offset)
- 가중치 알고리즘 적용
- Redis 캐싱
- API 테스트 (Unit + Integration)

---

### 구현 계획 (Week 1-4)

#### Week 1: 페이지네이션 인프라 구축

**1. 공통 DTO 생성**

파일 위치: `MS-Common/src/main/java/kr/suhsaechan/mapsy/common/dto/page/`

**작업 항목**:
- [ ] `PageResponse.java` - Offset 기반 페이지네이션 응답
- [ ] `PageInfo.java` - 페이지 메타데이터
- [ ] `SortInfo.java` - 정렬 정보
- [ ] `CursorPageResponse.java` - Cursor 기반 페이지네이션 응답
- [ ] `CursorInfo.java` - 커서 정보
- [ ] `Cursor.java` - 커서 인코딩/디코딩 (Base64)

**코드 예시 (PageResponse.java)**:
```java
@Getter
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class PageResponse<T> {
    private List<T> content;
    private PageInfo page;
    private SortInfo sort;

    public static <T> PageResponse<T> from(Page<T> page) {
        return PageResponse.<T>builder()
            .content(page.getContent())
            .page(PageInfo.from(page))
            .sort(SortInfo.from(page.getSort()))
            .build();
    }
}
```

**2. 페이지네이션 유틸리티**

파일: `MS-Common/src/main/java/kr/suhsaechan/mapsy/common/util/PageUtil.java`

**작업 항목**:
- [ ] `createPageable()` - Pageable 생성 (validation 포함)
- [ ] `createSort()` - Sort 생성 ("field,direction" 문자열 파싱)
- [ ] `validatePage()` - 페이지 번호 검증
- [ ] `validateSize()` - 페이지 크기 검증 (default: 20, max: 100)
- [ ] `validateCursorSize()` - 커서 크기 검증 (max: 50)

**3. ErrorCode 추가**

파일: `MS-Common/src/main/java/kr/suhsaechan/mapsy/common/exception/constant/ErrorCode.java`

```java
INVALID_PAGE_PARAMETER(HttpStatus.BAD_REQUEST, "유효하지 않은 페이지 번호"),
INVALID_SORT_PARAMETER(HttpStatus.BAD_REQUEST, "유효하지 않은 정렬 파라미터"),
INVALID_CURSOR(HttpStatus.BAD_REQUEST, "유효하지 않은 커서"),
```

---

#### Week 2: Repository 페이지네이션 메서드 추가

**1. PlaceRepository 확장**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/repository/PlaceRepository.java`

**작업 항목**:
- [ ] `findAllByOrderByCreatedAtDesc(Pageable)` - 최신 장소 (Offset)
- [ ] `findAllAfterCursor(LocalDateTime, UUID, Pageable)` - 최신 장소 (Cursor)
- [ ] `findAllOrderByPopularity(LocalDateTime, Pageable)` - 인기 장소 (가중치)
- [ ] `findPopularPlacesAfterCursor(...)` - 인기 장소 (Cursor + 가중치)
- [ ] `findByKeyword(String, Pageable)` - 키워드별 장소

**코드 예시 (가중치 적용)**:
```java
@Query("SELECT p, " +
       "(SUM(CASE WHEN mp.createdAt >= :since THEN 2 ELSE 1 END)) as weightedScore " +
       "FROM Place p " +
       "LEFT JOIN MemberPlace mp ON mp.place = p AND mp.deletedAt IS NULL " +
       "WHERE mp.createdAt IS NOT NULL " +
       "GROUP BY p " +
       "ORDER BY weightedScore DESC, p.createdAt DESC")
Page<Place> findAllOrderByPopularity(@Param("since") LocalDateTime since, Pageable pageable);
```

**2. MemberPlaceRepository 확장**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/repository/MemberPlaceRepository.java`

**작업 항목**:
- [ ] `findByMemberAndSavedStatusWithPlace(...)` - 저장 장소 (Offset)
- [ ] `findByMemberAndSavedStatusAfterCursor(...)` - 저장 장소 (Cursor)
- [ ] `findByMemberAndSavedStatusOrderByPopularity(...)` - 내 저장 TOP

**3. KeywordRepository 확장**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/repository/KeywordRepository.java`

**작업 항목**:
- [ ] `findTopByTrendScore(Pageable)` - 트렌드 키워드 (이미 존재)
- [ ] 성능 최적화: 인덱스 확인 (`keyword`, `trendScore DESC`)

---

#### Week 3: Service Layer 구현

**1. FeedService 신규 생성**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/service/FeedService.java`

**작업 항목**:
- [ ] `getLatestPlacesCursor(String cursor, Integer size)` - 최신 장소 피드
- [ ] `getPopularPlacesCursor(String cursor, Integer size)` - 인기 장소 피드
- [ ] `getMyTopPlaces(UUID memberId, Integer limit)` - 내 저장 TOP

**코드 구조**:
```java
@Service
@RequiredArgsConstructor
@Transactional(readOnly = true)
public class FeedService {
    private final PlaceRepository placeRepository;
    private final MemberPlaceRepository memberPlaceRepository;

    public CursorPageResponse<PlaceDto> getLatestPlacesCursor(String cursor, Integer size) {
        // 1. Cursor 디코딩 (null이면 첫 요청)
        // 2. Repository 호출 (size + 1 조회)
        // 3. hasNext 계산
        // 4. CursorInfo 생성 (마지막 항목의 ID + Timestamp 인코딩)
        // 5. CursorPageResponse 반환
    }
}
```

**2. KeywordService 확장**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/service/KeywordService.java`

**작업 항목**:
- [ ] `getTrendingKeywords(Integer limit, Integer days)` - 트렌드 키워드
- [ ] `getPlacesByKeyword(String keyword, Integer page, Integer size, String sort)` - 키워드별 장소
- [ ] `getKeywordDetail(String keyword)` - 키워드 상세

**3. MemberPlaceService 확장**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/service/MemberPlaceService.java`

**작업 항목**:
- [ ] `getSavedPlacesCursor(UUID memberId, String cursor, Integer size)` - 저장 장소 (Cursor)
- [ ] `getSavedPlaces(...)` - 저장 장소 (Offset)
- [ ] `getTemporaryPlaces(...)` - 임시 저장 장소

---

#### Week 4: Controller API 엔드포인트 구현

**1. FeedController 신규 생성**

파일: `MS-Web/src/main/java/kr/suhsaechan/mapsy/web/controller/FeedController.java`

**엔드포인트**:
```java
@RestController
@RequestMapping("/api/feed")
@Tag(name = "피드 관리")
public class FeedController {

    @GetMapping("/latest/cursor")
    public ResponseEntity<CursorPageResponse<PlaceDto>> getLatestPlaces(
        @RequestParam(required = false) String cursor,
        @RequestParam(required = false) Integer size) {
        // FeedService.getLatestPlacesCursor() 호출
    }

    @GetMapping("/popular")
    public ResponseEntity<CursorPageResponse<PlaceDto>> getPopularPlaces(
        @RequestParam(required = false) String cursor,
        @RequestParam(required = false) Integer size) {
        // FeedService.getPopularPlacesCursor() 호출
    }

    @GetMapping("/my-top-places")
    public ResponseEntity<List<PlaceDto>> getMyTopPlaces(
        @AuthenticationPrincipal CustomUserDetails userDetails,
        @RequestParam(defaultValue = "10") Integer limit) {
        // FeedService.getMyTopPlaces() 호출
    }

    @GetMapping("/trending-keywords")
    public ResponseEntity<List<KeywordDto>> getTrendingKeywords(
        @RequestParam(defaultValue = "20") Integer limit,
        @RequestParam(defaultValue = "7") Integer days) {
        // KeywordService.getTrendingKeywords() 호출
    }
}
```

**2. KeywordController 신규 생성**

파일: `MS-Web/src/main/java/kr/suhsaechan/mapsy/web/controller/KeywordController.java`

**엔드포인트**:
- GET `/api/keywords/{keyword}` - 키워드 상세
- GET `/api/keywords/{keyword}/places` - 키워드별 장소

**3. BookmarkController 신규 생성**

파일: `MS-Web/src/main/java/kr/suhsaechan/mapsy/web/controller/BookmarkController.java`

**엔드포인트**:
- GET `/api/bookmarks` - 저장 장소 (Offset)
- GET `/api/bookmarks/cursor` - 저장 장소 (Cursor)
- GET `/api/bookmarks/temporary` - 임시 저장 장소
- POST `/api/bookmarks/{placeId}/save` - 장소 저장
- PATCH `/api/bookmarks/{bookmarkId}` - 북마크 수정

---

#### Redis 캐싱 적용

**1. Redis 설정 확인**

파일: `MS-Web/src/main/resources/application.yml`

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: {PASSWORD}
```

**2. 트렌드 키워드 캐싱**

파일: `MS-Place/src/main/java/kr/suhsaechan/mapsy/place/service/KeywordService.java`

```java
@Cacheable(value = "trending-keywords", key = "#limit + ':' + #days")
public List<KeywordDto> getTrendingKeywords(Integer limit, Integer days) {
    // 기존 로직
}
```

**Cache 설정**:
- Key: `trending-keywords:{limit}:{days}`
- TTL: 1시간 (3600초)
- Eviction: 매시간 자동 갱신

---

### 테스트 작성

**1. Unit Test (JUnit 5 + Mockito)**

파일 예시: `MS-Place/src/test/java/kr/suhsaechan/mapsy/place/service/FeedServiceTest.java`

**테스트 항목**:
- [ ] `getLatestPlacesCursor_첫_요청_성공()`
- [ ] `getLatestPlacesCursor_다음_페이지_요청_성공()`
- [ ] `getLatestPlacesCursor_마지막_페이지_hasNext_false()`
- [ ] `getPopularPlacesCursor_가중치_정렬_확인()`
- [ ] `getTrendingKeywords_캐시_적용_확인()`

**2. Integration Test (MockMvc)**

파일 예시: `MS-Web/src/test/java/kr/suhsaechan/mapsy/web/controller/FeedControllerTest.java`

**테스트 항목**:
- [ ] GET `/api/feed/latest/cursor` - 200 OK
- [ ] GET `/api/feed/popular` - 가중치 점수 확인
- [ ] GET `/api/feed/trending-keywords` - Redis 캐시 확인

---

## 🤖 AI 서버 안정화 가이드

### 현재 상태 (60% 완료)

**✅ 완료된 항목**:
- FastAPI 서버 기본 구조
- Instagram/YouTube 콘텐츠 다운로드 (yt-dlp)
- 음성 추출 (ffmpeg) + STT (Whisper)
- LLM 분석 (Gemini 2.5 Flash)
- Geocoding (Kakao Maps API)
- Callback 메커니즘

**❌ 미완성 항목**:
- OCR 기능 (비활성화 상태)
- TikTok/Facebook/Twitter 지원
- 자동 좌표 추출
- 추출 정확도 향상
- 에러 핸들링 강화

---

### 구현 계획 (Week 2-5)

#### Week 2: OCR 기능 재활성화

**1. 비디오 OCR 복원**

파일: `MapSee-AI/src/services/video.py`

**작업 항목**:
- [ ] 프레임 추출 (2 FPS)
- [ ] ROI 추출 (하단 40%)
- [ ] Perceptual Hashing 중복 제거
- [ ] Tesseract OCR 적용 (한글 언어팩)
- [ ] 추출된 텍스트 정리 (공백 제거, 중복 제거)

**코드 예시**:
```python
async def extract_video_text(video_stream: BytesIO) -> str:
    frames = extract_frames_at_fps(video_stream, fps=2)
    roi_frames = [extract_roi(frame, bottom_percent=40) for frame in frames]
    unique_frames = deduplicate_frames(roi_frames)
    texts = [tesseract_ocr(frame, lang='kor') for frame in unique_frames]
    return clean_text(" ".join(texts))
```

**2. 이미지 OCR 추가**

파일: `MapSee-AI/src/services/image.py`

**작업 항목**:
- [ ] Instagram 캐러셀 이미지 처리
- [ ] 이미지 전체 OCR (Tesseract)
- [ ] 텍스트 영역 감지 (EAST/CRAFT)
- [ ] 장소명 추출 패턴 (정규표현식)

---

#### Week 3: 플랫폼 확장

**1. TikTok 지원**

파일: `MapSee-AI/src/services/sns.py`

**작업 항목**:
- [ ] TikTok URL 패턴 감지
- [ ] yt-dlp TikTok extractor 설정
- [ ] 캡션 추출
- [ ] 음성 추출 + STT

**2. Facebook/Twitter 지원**

**작업 항목**:
- [ ] Facebook URL 패턴 감지
- [ ] Twitter/X URL 패턴 감지
- [ ] Playwright 기반 스크래핑 (yt-dlp 불가 시)
- [ ] 캡션/본문 추출

---

#### Week 4: 자동 좌표 추출

**1. LLM 프롬프트 개선**

파일: `MapSee-AI/src/services/llm.py`

**현재 프롬프트**:
```
영상/게시물에서 장소 정보 추출:
- name: 장소 이름
- address: 주소
- description: 설명
```

**개선 프롬프트**:
```
영상/게시물에서 장소 정보 추출:
- name: 장소 이름
- address: 도로명 주소 (예: 서울특별시 종로구 삼청로 123)
- latitude: 위도 (주소에서 추론 가능하면 제공)
- longitude: 경도 (주소에서 추론 가능하면 제공)
- description: 장소 설명

주의사항:
- 주소는 반드시 도로명 주소 형식
- 위도/경도는 확실한 경우만 제공
- 없으면 null 반환
```

**2. Geocoding 통합**

파일: `MapSee-AI/src/services/geocoding.py`

**로직**:
1. LLM이 좌표 제공 → 그대로 사용
2. LLM이 좌표 미제공 → Kakao Geocoding API 호출
3. Kakao 실패 → Nominatim Fallback

---

#### Week 5: 추출 정확도 향상

**1. LLM 프롬프트 튜닝**

**테스트 데이터셋 구축**:
- Instagram 릴스 50개 (맛집/카페 언급)
- YouTube 쇼츠 50개 (여행/장소 소개)

**목표 정확도**:
- Instagram: 85%+ (현재 추정 70%)
- YouTube: 80%+ (현재 추정 65%)

**개선 방향**:
- 컨텍스트 강화: "이 영상은 맛집 소개 영상입니다" 추가
- Few-shot 예시 제공: 좋은 추출 결과 3개 포함
- JSON Schema 강제: Pydantic 모델 엄격화

**2. 에러 핸들링 강화**

파일: `MapSee-AI/src/main.py`

**작업 항목**:
- [ ] SNS 다운로드 실패 → 명확한 에러 메시지
- [ ] STT 실패 → 캡션만 사용하여 계속 진행
- [ ] LLM 응답 파싱 실패 → 재시도 로직 (최대 3회)
- [ ] Callback 실패 → 큐에 저장 후 재전송

---

### 테스트 작성

**1. 추출 정확도 테스트**

파일: `MapSee-AI/tests/test_extraction_accuracy.py`

```python
@pytest.mark.parametrize("url,expected_places", [
    ("https://instagram.com/p/ABC123", ["카페 온더무드"]),
    ("https://youtube.com/shorts/XYZ789", ["명동 교자", "광장시장"]),
])
async def test_extraction_accuracy(url, expected_places):
    result = await extract_places(url)
    extracted_names = [p["name"] for p in result["places"]]
    assert all(place in extracted_names for place in expected_places)
```

**2. 성능 테스트**

**목표**:
- 평균 처리 시간: 30초 이내
- 동시 처리: 10개 요청 처리 가능

---

## 📱 Flutter 앱 개발 가이드

### 현재 상태 (0% - 미시작)

**필요 작업**:
- 프로젝트 초기 설정
- UI/UX 디자인 시스템 정의
- 6개 주요 화면 구현
- API 연동
- 상태 관리
- FCM 푸시 알림
- 지도 연동

---

### 구현 계획 (Week 3-8)

#### Week 3: 프로젝트 초기 설정

**1. Flutter 프로젝트 생성**

```bash
flutter create mapsy_app
cd mapsy_app
```

**2. 패키지 의존성 추가**

파일: `pubspec.yaml`

```yaml
dependencies:
  flutter:
    sdk: flutter

  # State Management
  riverpod: ^2.5.0
  flutter_riverpod: ^2.5.0

  # API Communication
  dio: ^5.4.0
  retrofit: ^4.0.3
  json_annotation: ^4.8.1

  # Firebase
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  firebase_messaging: ^14.7.6

  # UI Components
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  lottie: ^2.7.0

  # Map
  google_maps_flutter: ^2.5.0
  url_launcher: ^6.2.2

  # Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  build_runner: ^2.4.7
  retrofit_generator: ^8.0.6
  json_serializable: ^6.7.1
```

**3. 프로젝트 구조**

```
lib/
├── main.dart
├── app.dart
├── core/
│   ├── constants/
│   ├── themes/
│   ├── utils/
│   └── widgets/
├── features/
│   ├── auth/
│   │   ├── data/
│   │   ├── domain/
│   │   └── presentation/
│   ├── home/
│   ├── place/
│   ├── bookmark/
│   └── keyword/
└── shared/
    ├── models/
    ├── providers/
    └── services/
```

---

#### Week 4-5: UI/UX 디자인 시스템 정의

**1. 컬러 팔레트**

파일: `lib/core/constants/app_colors.dart`

```dart
class AppColors {
  static const primary = Color(0xFFFF6B6B);
  static const secondary = Color(0xFF4ECDC4);
  static const background = Color(0xFFF7F7F7);
  static const text = Color(0xFF2D3436);
  static const textSecondary = Color(0xFF636E72);
  static const error = Color(0xFFD63031);
}
```

**2. 타이포그래피**

파일: `lib/core/themes/app_text_styles.dart`

```dart
class AppTextStyles {
  static const h1 = TextStyle(fontSize: 24, fontWeight: FontWeight.bold);
  static const h2 = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);
  static const body1 = TextStyle(fontSize: 16, fontWeight: FontWeight.normal);
  static const caption = TextStyle(fontSize: 12, color: AppColors.textSecondary);
}
```

**3. 공통 위젯**

파일: `lib/core/widgets/`

**작업 항목**:
- [ ] `AppButton.dart` - 기본 버튼
- [ ] `AppTextField.dart` - 입력 필드
- [ ] `PlaceCard.dart` - 장소 카드 (리스트용)
- [ ] `PlaceGridItem.dart` - 장소 카드 (그리드용)
- [ ] `KeywordChip.dart` - 키워드 칩
- [ ] `LoadingIndicator.dart` - 로딩 인디케이터
- [ ] `EmptyState.dart` - 빈 상태 UI

---

#### Week 5-6: 주요 화면 구현

**1. 온보딩 화면**

파일: `lib/features/auth/presentation/onboarding_screen.dart`

**작업 항목**:
- [ ] 스플래시 화면 (Lottie 애니메이션)
- [ ] 온보딩 슬라이드 (PageView)
- [ ] 로그인/회원가입 (Firebase OAuth)
- [ ] 초기 정보 입력 (생년월일/성별/닉네임)

**2. 홈 화면 (피드)**

파일: `lib/features/home/presentation/home_screen.dart`

**작업 항목**:
- [ ] 헤더 (로고 + 알림)
- [ ] 최신 추가 장소 섹션 (무한 스크롤)
- [ ] 인기 인스타 피드 (그리드 3열)
- [ ] 내 저장 장소 TOP (가로 스크롤)
- [ ] 트렌드 키워드 (칩 형태)
- [ ] FAB (URL 추가)

**3. AI 추출 화면**

파일: `lib/features/place/presentation/extraction_screen.dart`

**작업 항목**:
- [ ] URL 입력 바텀 시트
- [ ] 처리 중 화면 (Lottie)
- [ ] 추출 완료 화면 (장소 리스트)
- [ ] 추출 실패 화면

**4. 키워드 피드 화면**

파일: `lib/features/keyword/presentation/keyword_feed_screen.dart`

**작업 항목**:
- [ ] 키워드 정보 카드
- [ ] 필터/정렬 바
- [ ] 그리드뷰/리스트뷰 전환
- [ ] 무한 스크롤

**5. 장소 상세 화면**

파일: `lib/features/place/presentation/place_detail_screen.dart`

**작업 항목**:
- [ ] 헤더 이미지
- [ ] 기본 정보
- [ ] 영업 정보
- [ ] 지도 미리보기
- [ ] 키워드 칩
- [ ] 출처 콘텐츠
- [ ] 하단 저장/공유 버튼

**6. 북마크 관리 화면**

파일: `lib/features/bookmark/presentation/bookmark_screen.dart`

**작업 항목**:
- [ ] 탭 바 (폴더별)
- [ ] 정렬/필터 바
- [ ] 장소 리스트
- [ ] 스와이프 액션
- [ ] 바텀 시트 메뉴

---

#### Week 7: API 연동

**1. Retrofit API Client 정의**

파일: `lib/shared/services/api_client.dart`

```dart
@RestApi(baseUrl: "https://api.mapsy.suhsaechan.kr")
abstract class ApiClient {
  factory ApiClient(Dio dio) = _ApiClient;

  @GET("/api/feed/latest/cursor")
  Future<CursorPageResponse<PlaceDto>> getLatestPlaces(
    @Query("cursor") String? cursor,
    @Query("size") int? size,
  );

  @GET("/api/feed/popular")
  Future<CursorPageResponse<PlaceDto>> getPopularPlaces(
    @Query("cursor") String? cursor,
    @Query("size") int? size,
  );

  @POST("/api/content/analyze")
  Future<ContentResponse> analyzeContent(@Body() AnalyzeRequest request);
}
```

**2. Riverpod Provider 설정**

파일: `lib/shared/providers/api_provider.dart`

```dart
final dioProvider = Provider<Dio>((ref) {
  final dio = Dio(BaseOptions(baseUrl: "https://api.mapsy.suhsaechan.kr"));
  dio.interceptors.add(AuthInterceptor(ref));
  return dio;
});

final apiClientProvider = Provider<ApiClient>((ref) {
  return ApiClient(ref.watch(dioProvider));
});
```

**3. 상태 관리 (Riverpod)**

파일: `lib/features/home/providers/latest_places_provider.dart`

```dart
final latestPlacesProvider = StateNotifierProvider<LatestPlacesNotifier, AsyncValue<List<PlaceDto>>>(
  (ref) => LatestPlacesNotifier(ref.watch(apiClientProvider)),
);

class LatestPlacesNotifier extends StateNotifier<AsyncValue<List<PlaceDto>>> {
  final ApiClient _apiClient;
  String? _cursor;

  LatestPlacesNotifier(this._apiClient) : super(const AsyncValue.loading()) {
    fetchInitial();
  }

  Future<void> fetchInitial() async {
    state = const AsyncValue.loading();
    try {
      final response = await _apiClient.getLatestPlaces(null, 20);
      _cursor = response.cursor.next;
      state = AsyncValue.data(response.content);
    } catch (e, st) {
      state = AsyncValue.error(e, st);
    }
  }

  Future<void> fetchMore() async {
    if (_cursor == null) return;
    try {
      final response = await _apiClient.getLatestPlaces(_cursor, 20);
      _cursor = response.cursor.hasNext ? response.cursor.next : null;
      state = AsyncValue.data([...state.value!, ...response.content]);
    } catch (e) {
      // 에러 처리
    }
  }
}
```

---

#### Week 8: FCM 푸시 알림 + 지도 연동

**1. FCM 설정**

파일: `lib/core/services/fcm_service.dart`

```dart
class FcmService {
  final FirebaseMessaging _messaging = FirebaseMessaging.instance;

  Future<void> initialize() async {
    // 권한 요청
    await _messaging.requestPermission();

    // 토큰 획득
    final token = await _messaging.getToken();
    print('FCM Token: $token');

    // 포그라운드 메시지 처리
    FirebaseMessaging.onMessage.listen((RemoteMessage message) {
      // 로컬 알림 표시
    });

    // 백그라운드 메시지 처리
    FirebaseMessaging.onMessageOpenedApp.listen((RemoteMessage message) {
      // 화면 이동
    });
  }
}
```

**2. 지도 연동**

파일: `lib/features/place/widgets/place_map.dart`

```dart
class PlaceMap extends StatelessWidget {
  final double latitude;
  final double longitude;

  @override
  Widget build(BuildContext context) {
    return GoogleMap(
      initialCameraPosition: CameraPosition(
        target: LatLng(latitude, longitude),
        zoom: 15,
      ),
      markers: {
        Marker(
          markerId: MarkerId('place'),
          position: LatLng(latitude, longitude),
        ),
      },
    );
  }
}
```

**3. 길찾기 버튼**

```dart
Future<void> _openKakaoMap(double lat, double lng) async {
  final url = 'kakaomap://look?p=$lat,$lng';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    // 웹 버전으로 fallback
    await launch('https://map.kakao.com/link/map/$lat,$lng');
  }
}
```

---

### 테스트 작성

**1. Widget Test**

파일: `test/features/home/home_screen_test.dart`

```dart
testWidgets('홈 화면에 최신 장소 표시', (WidgetTester tester) async {
  await tester.pumpWidget(ProviderScope(child: MyApp()));

  // 최신 장소 섹션 확인
  expect(find.text('🆕 방금 추가된 장소'), findsOneWidget);

  // 장소 카드 확인
  expect(find.byType(PlaceCard), findsWidgets);
});
```

**2. Integration Test**

파일: `integration_test/app_test.dart`

```dart
testWidgets('AI 추출 플로우', (WidgetTester tester) async {
  // 1. FAB 클릭
  await tester.tap(find.byType(FloatingActionButton));
  await tester.pumpAndSettle();

  // 2. URL 입력
  await tester.enterText(find.byType(TextField), 'https://instagram.com/p/ABC123');
  await tester.tap(find.text('추출 시작'));
  await tester.pumpAndSettle();

  // 3. 처리 완료 대기
  await tester.pump(Duration(seconds: 30));

  // 4. 추출된 장소 확인
  expect(find.text('✅ N개의 장소를 찾았어요!'), findsOneWidget);
});
```

---

## 📊 통합 테스트 시나리오

### E2E 테스트 플로우

**시나리오 1: 신규 사용자 온보딩 → AI 추출 → 저장**
```
1. 앱 실행 → 스플래시 → 온보딩
2. Google 로그인 → 초기 정보 입력
3. 홈 화면 진입
4. FAB 클릭 → Instagram URL 입력
5. AI 추출 완료 → 장소 선택
6. 폴더 선택 → 저장 완료
7. 북마크 화면에서 저장된 장소 확인
```

**시나리오 2: 키워드 탐색 → 장소 상세 → 저장**
```
1. 홈 화면 → 트렌드 키워드 "#성수동카페" 클릭
2. 키워드 피드 → 필터 "인기순" 선택
3. 첫 번째 장소 클릭 → 상세 화면
4. 정보 확인 (주소/영업시간/지도)
5. 저장 버튼 → 폴더 선택
6. 메모/별점 입력 → 저장 완료
```

**시나리오 3: 북마크 관리 → 방문 기록**
```
1. 북마크 탭 → "가고 싶은 곳" 선택
2. 장소 메뉴 클릭 → "방문 날짜 기록"
3. 캘린더에서 날짜 선택
4. "가본 곳" 탭으로 자동 이동 확인
5. 방문함 배지 표시 확인
```

---

## 🚀 배포 및 운영

### 배포 전 체크리스트

**Backend**:
- [ ] 환경변수 설정 (Firebase, JWT Secret, API Keys)
- [ ] DB 마이그레이션 적용
- [ ] Redis 서버 실행
- [ ] API 문서 최신화 (Swagger)
- [ ] 로그 설정 (Sentry 연동)

**AI Server**:
- [ ] 환경변수 설정 (Gemini, YouTube, Kakao API Keys)
- [ ] ffmpeg/ffprobe 설치 확인
- [ ] Tesseract 한글 언어팩 설치
- [ ] GPU 사용 설정 (선택적)

**Flutter App**:
- [ ] Firebase 프로젝트 설정 (iOS/Android)
- [ ] Google Maps API Key 설정
- [ ] FCM 설정
- [ ] App Store/Play Store 등록 준비

### 모니터링

**메트릭**:
- API 응답 시간 (P50/P95/P99)
- AI 추출 성공률 (목표: Instagram 85%+, YouTube 80%+)
- 평균 처리 시간 (목표: 30초 이내)
- 에러율 (목표: 1% 이하)
- DAU/MAU

**도구**:
- Sentry (에러 트래킹)
- Firebase Analytics (사용자 행동)
- Grafana (시스템 메트릭)

---

## 📌 참고사항

### 개발 가이드라인

**코드 리뷰 체크리스트**:
- [ ] PRD 요구사항 충족 여부
- [ ] 테스트 커버리지 80% 이상
- [ ] API 명세서 일치 여부
- [ ] 에러 처리 완료 여부
- [ ] 민감 정보 마스킹 확인

**Git 브랜치 전략**:
```
main (프로덕션)
  └─ develop (개발)
       ├─ feature/feed-api (피드 API)
       ├─ feature/ai-ocr (AI OCR)
       └─ feature/flutter-home (Flutter 홈)
```

**커밋 메시지 컨벤션**:
```
feat: 피드 API 페이지네이션 구현
fix: AI 추출 실패 시 에러 처리
docs: API 문서 업데이트
test: 피드 서비스 단위 테스트 추가
```

---

**문서 버전**: v1.0
**마지막 업데이트**: 2026-01-19
**작성자**: Claude (Anthropic AI)

---

**End of Implementation Guide**
